<Application x:Class="_9567A_V00___PI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:_9567A_V00___PI" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             StartupUri="TelaInicial.xaml">
    <Application.Resources>
        <ControlTemplate x:Key="CalendarControlTemplate1" TargetType="{x:Type Calendar}">
            <AdornerDecorator CacheMode="{Binding (materialDesign:ShadowAssist.CacheMode), RelativeSource={RelativeSource Self}}">
                <Border Effect="{DynamicResource MaterialDesignShadowDepth4}" Padding="16,8,16,24">
                    <CalendarItem x:Name="PART_CalendarItem" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}" Style="{DynamicResource MaterialDesignCalendarItemPortrait}"/>
                </Border>
            </AdornerDecorator>
        </ControlTemplate>
    </Application.Resources>
</Application>
